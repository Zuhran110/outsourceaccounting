const arr = [1, 2, 3, 4, 5];

const bubble_sort = () => {
  for (let i = 0; i < array.length; i++) {
    const element = array[i];
    for (let j = 0; j < array.length; j++) {
      const element = array[j];
      if (i < j) {
        let temp = i;
        i = j;
        j = temp;
      }
    }
  }
};
